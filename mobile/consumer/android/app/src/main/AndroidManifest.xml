<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="تجربتي"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- FD-M7 (2026-09-21): Android App Links — the campaign QR
                 encodes https://<api-host>/app/consumer/?qr=<code>, where
                 <api-host> is whatever host serves the CURRENT API (the QR
                 URL is generated from the request host — see
                 api/src/routes/company.ts). __APP_LINKS_HOST__ is replaced
                 at build time with the host derived from CURRENT_API_BASE
                 (see .github/workflows/build-consumer-current.yml); it is
                 intentionally NOT the legacy Railway API host. Until a
                 real host is injected the token matches nothing and the
                 web Consumer remains the fallback. autoVerify requires
                 /.well-known/assetlinks.json on that domain naming this
                 package + release cert SHA-256 (served by the API; see
                 api/src/server.ts). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="__APP_LINKS_HOST__"
                      android:pathPrefix="/app/consumer" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
