{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd api && npm install && npm run db:generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd api && npm run db:migrate && npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
